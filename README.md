Please look at the wiki page to learn how to use! (May be incomplete so look at the code too!)

## How to build
1. Install git, python3, and g++
2. Run `git clone https://github.com/MeanTheBean/mtb-lang`
3. `cd` into `mtb-lang` and run `./build.sh`
4. Compiled binary will be called `mtb`
